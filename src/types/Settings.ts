export interface Settings {
    hasVisited: boolean
    defaultSettings: boolean
    useTimer: boolean
    rememberTime: number
    dropTime: number
    sortTiles: boolean
    invertTiles: boolean
    hideTiles: boolean
}

export interface SettingsType {
    hasVisited?: boolean
    defaultSettings?: boolean
    useTimer?: boolean
    rememberTime?: number
    dropTime?: number
    sortTiles?: boolean
    invertTiles?: boolean
    hideTiles?: boolean
}