export interface Settings {
    hasVisited: boolean
    defaultSettings: boolean
    useTimer: boolean
    sortTiles: boolean
    invertTiles: boolean
    hideTiles: boolean
}

export interface SettingsType {
    hasVisited?: boolean
    defaultSettings?: boolean
    useTimer?: boolean
    sortTiles?: boolean
    invertTiles?: boolean
    hideTiles?: boolean
}